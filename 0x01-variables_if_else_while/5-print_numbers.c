#include <stdio.h>

/**
  * main - this code will print the 10 digit numbers
  *
  * Return: Always (Success)
  */
int main(void)
{
	int a = 0;
	while (a <= 9)
	{
	    printf("%d", a);
	    a++;
	}
	return (0);

}
